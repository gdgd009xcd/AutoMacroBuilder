1. ブランチの作成

1)Git->分岐/タグ->分岐の作成...

2) 分岐の作成ダイアログ

3) 分岐名、作成した分岐をチェックアウト
　　以後、ブランチがカレント(HEAD)になる。

2. ブランチのmasterへのマージ

1) masterをチェックアウトする。
　　ローカルのマスターを選択して、チェックアウト。

2) masterにブランチをマージする。
　Git->分岐/タグ->リビジョンをマージ

3) ローカルのブランチを選択し、マージ。
　　※ masterは、チェックアウトした状態であること。
　　　　修正すると、マージは失敗(キャンセル)する。
